import * as React from "react";
import * as ReactDOM from "react-dom";
import App from "./App";
import "./styles.css";
import "./reach-styles";

// Open App.tsx to add your code If you would prefer not to write TypeScript,
// rename App.tsx to App.js, refresh the CodeSandbox browser and your code
// should still work! We ❤️ CodeSandbox!

const rootElement = document.getElementById("root");
ReactDOM.render(<App />, rootElement);
