import Helmet from "react-helmet"

<Helmet
  title="Reach UI - useWindowSize, WindowSize"
  meta={[
    {
      name: "description",
      content: "Measure the current window dimensions",
    },
  ]}
/>

# @reach/window-size

[useWindowSize](#usewindowsize) -
[WindowSize](#windowsize)

Measure the current window dimensions.

## Installation

```bash
npm install @reach/window-size
# or
yarn add @reach/window-size
```

And then import it:

```js
import WindowSize, { useWindowSize } from "@reach/window-size"
```

# useWindowSize

```jsx
const { width, height } = useWindowSize()
```

Hook that observes and returns the measurements of the browser window.

# WindowSize

```jsx
<WindowSize>
  {size => (
    <div>
      <div>Resize your window.</div>
      <pre>{JSON.stringify(size, null, 2)}</pre>
    </div>
  )}
</WindowSize>
```

## Props

| Prop                  | Type | Required |
| --------------------- | ---- | -------- |
| [children](#children) | func | true     |

### children

_Type_: `func: (({ width, height })) => node`

A function that calls back to you with the window size.
