import adapt from "@reach/adapter";
import { tabs, tablist, tab, tabpanels, tabpanel } from "./defs";

export const Tabs = adapt(tabs);
export const TabList = adapt(tablist);
export const Tab = adapt(tab);
export const TabPanels = adapt(tabpanels);
export const TabPanel = adapt(tabpanel);
